> you can install typescript globally too but recommended to use it locally
> typescript SHOULD be a DEV DEPENDENCY
> "Tsc" is a common typescript compiler.We can also use "esbuild","swc" for the compilation process.
> TYPE INFERENCING
> npx tsc -b to build the ts file or compile the file
> index.js is the main file which will run on browser or in node.js
> index.d.ts is the typescript declaration file which contains only declarations not implementations
> index.js.map It maps compiled JS back to original TypeScript source.
> index.d.ts.map It maps ".d.ts" back to original ".ts"
> tsconfig.tsbuildinfo This file stores:
                    which files were compiled
                    dependency graph
                    timestamps
                    hashes
                    compiler state

                    so next build can reuse previous work.

Without it every build compile ALL files again

With .tsbuildinfo only changed files are recompiled

This makes builds MUCH faster in large projects.

EXAMPLE:
Suppose you have:
100 TypeScript files

You change only:
Navbar.tsx

Without .tsbuildinfo:
TypeScript recompiles everything.

With .tsbuildinfo:
TypeScript recompiles only affected files.

DON'T COMMIT THIS FILE

> For seeing your TS code working u can use "ts-node" package or u can first compile ts file then run the "node converTed_tsFile.js" cmd

> The type is being inferred by the TypeScript COMPILER

> TypeScript type inference is the compiler's built-in ability to automatically deduce data types without requiring you to write explicit type annotations. It allows you to write clean, concise JavaScript-like code while still completely preserving the benefits of strict static type safety.

ANNOTATION - mai samjhaunga
INFERENCE - mai khud samajh jaaunga

> void VS NULL VS undefined


> The Type System in TypeScript describes the various data types supported by the language. It is divided into three major sections: Any Type, Built-In Type, and User-Defined Type. 

> WHAT I WAS DOING WRONG ->>


You are calling Greet("Tushar") immediately instead of passing the function itself.

This line:

DelayedCall(Greet("Tushar"))

does this:

1. Calls Greet immediately
2. console.log runs
3. Greet returns undefined
4. undefined gets passed to DelayedCall

But DelayedCall expects:

fn: () => void

which means:

a function with no parameters



"() => Greet("Tushar")"" WHY THIS WORKS ->>

a function
takes no arguments
returns void

Exactly matching:
() => void


> Internals of TS.How it works/Compiler working

> tsconfig.json
    1.TARGET : The target setting changes which JS features are downleveled and which are left intact. For example, an arrow function () => this will be turned into an equivalent function expression if target is ES5 or lower.
    "mainly ye btata hai JS ka konsa version use kar rahe hai"
    2.rootDir:IN this we specify the path of the folder which contains the actual .Ts files not any other file.NAMING- "./src"
    3.outDir:In this we specify the path of the folder which contains Ts build files(.js,.map.dt,etc..) like this folder we might or might not want to push to github.
    NAMING - "./build" or "./dist"(distribution)

    INPUT FOLDER - "/src"
    OUTPUT FOLDER - "/dist" OR "/build"


> noIMPLICITAny should be false( if we make it TRUE then our variable will take type ANY if we do not specify it explicitly) 

> OBJECT is not a primitive type,its a complex type variable so we need to define it and pass it to a function.(OBJECT AS AN ARGUMENT)

>. TypeScript does not allow the + operator on a union type unless it can prove the operation is safe.

> AVOID "any" AS MUCH AS YOU CAN

> INTERFACES can use OTHER INTERFACES

> When you are implementing an interface to a class the MEMBERS OF THE INTERFACE HAS TO BE PRESENT IN THE CLASS,YOU CAN ADD MORE MEMBERS but THE INTERFACE MEMBERS HAS TO BE PRESENT in the CLASS.

> WHY DO WE NEED INTERFACES IF WE HAVE CLASSES
While classes can define object structures, interfaces are essential for defining "contracts" that describe what an object should look like without the overhead of implementation or runtime presence
  1.Zero "Weight" in JavaScript: Interfaces only exist while you are writing code in TypeScript. When your code is turned into JavaScript (transpiled), interfaces completely disappear. Classes, however, stay in your final code and make your file size larger.

interfaces:

  Don't exist in memory
  Don't create objects
  Don't have constructors
  Don't have implementation

  Why do interfaces exist then?

Because sometimes you want to describe a shape without creating a class.

The main reason interfaces exist is not because they don't take memory.

In INTERFACES we cannot ACCESS THE MEMBER IN FUNCTION DEFINED IN THE INTERFACE UNLIKE CLASSES.

The main reason is:
To define and check the shape of data without needing a class.

Classes:

  Exist at runtime
  Occupy memory
  Can have methods
  Can have constructors
  Can create objects

> WE CAN IMPLEMENT TYPE ON A CLASS BUT IN LIMITED CASES(when statically defined datatypes like string,number etc.) BUT WHEN IT COMES TO "CUSTOM TYPE" THEN IT CREATES PROBLEM WITH CLASSES.THIS IS WHERE INTERFACE COMES INTO THE PICTURE

> An interface is a compile-time contract that defines the shape of an object without creating any real object or taking memory at runtime.You can't have default implementation of any method in the interface but can do in Abstract class/class.

Your version:

"Interfaces are kind of classes without memory"

is partially true as a learning analogy, but technically incorrect because:

Interface ≠ Class

    A class can:

    have constructors
    have implementation
    create objects

    An interface can do none of these.

> WHAT IS THE NEED TO IMPLEMENT AND INTERFACE WITH A CLASS and this case isn't it repeating again and again.(You violate DRY when you write the SAME LOGIC again and again)

> You can IMPLEMENT and EXTENDS in the same class.

> IF YOU HAVE A BASIC "TYPE" OBJECT THEN IT CAN BE IMPLEMENTED BY A CLASS IF IT IS A UNION(CUSTOM DATATYPE,this|that) THEN COMPILER WILL GENERATE ERROR.AND THIS IS THE MAIN USE OF INTERFACE (| = UNION, & = intersection)

> We can define a type alias for a primitive type like so:

  type Address = string;

  We often combine primitive type with union type to define a type alias, to make the code more readable:

  type NullOrUndefined = null | undefined;

  But we can’t use an interface to alias a primitive type. The interface can only be used for an object type.

> Blueprint refers to a Class: A template that defines the initial state (attributes) and behaviors (methods) of objects.It creates instances (objects). It provides the exact layout of data in memory and the default behaviors.

  Contract refers to an Interface : A strict set of rules declaring what a class should do, without specifying how to do it.A contract is a strict agreement. If a class adopts a contract (implements an interface), it promises to provide code for all the methods listed in that contract.NO OBJECT CREATION THROUGH THIS,COMPILE TIME ONLY

  Structure refers to Architecture: The overall hierarchy and organization of your software’s classes, packages, and relationships.


> Union types

Union types allow us to describe values that can be one of several types and create unions of various primitive, literal, or complex types:

type Transport = 'Bus' | 'Car' | 'Bike' | 'Walk';
Union type can only be defined using type.


> An abstract class is a partially implemented class that cannot be instantiated and is meant to be inherited by other classes.

EXAMPLE :  

abstract class Animal {
    abstract speak(): void;

    eat() {
        console.log("Eating");
    }
}

Here:

eat() is already implemented.
speak() must be implemented by child classes.
You cannot do:
  new Animal(); // ❌ Error

  Memory Trick
  Interface = Contract
  Abstract Class = Half-built Class
  Class = Complete Class


> Abstract Class

A half-implemented class that provides some code and forces subclasses to complete the rest.

Abstract Class = Rules + Some Code

.You can define methods in abstract class like you can have the default implementation of the methods in the abstract classes BUT YOU CAN'T DO THIS IN INTERFACE.

> WE SHOULD AVOID USING "arrow functions" INSIDE THE CLASSES AND OBJECTS as there will be a problem.Arrow functions dont HAVE THEIR OWN THIS

> If the parent class constructor already sets name and age, then you do not need to do it again in the child class.

    super(...) already initializes the parent fields
        ↓
    Don't assign them again

    If the parent constructor handles the initialization, let it do the work. That's one of the main benefits of inheritance.

    When would you need this.name = name?

    Only if the parent constructor doesn't initialize those properties.

> TYPE lets you do the UNION and INTERSECTION
  Interface is mainly for describing object/class structures, while type can describe objects plus unions, primitives, tuples, and function types.

> We can pick EXACTLY ONE TYPE in union types too.There are ways

> A union type (A | B) behaves like an intersection when accessing properties, because TypeScript only allows properties that are common to all members of the union.
Union of values
↓
Intersection of accessible properties

A & B

Values:       A AND B
Properties:   All from A + All from B


> Interview Answer
Use interface for object contracts and type when you need unions, intersections, or other advanced type compositions.

>let currentOrder: string | undefined;
This means that we are telling TS compiler ke "ya toh string aayegi ya undefined".WHY UNDEFINED ? WHAT IF THE GIVEN VALUE WILL NOT FOUND ? "then our variable contains UNDEFINED but we have already told TS that it's gonna be a string so that is why we need to give UNDEFINED type too"

>Properties can also be marked as readonly for TypeScript. While it won’t change any behavior at runtime, a property marked as readonly can’t be written to during type-checking.Using the readonly modifier doesn’t necessarily imply that a value is totally immutable - or in other words, that its internal contents can’t be changed. It just means the property itself can’t be re-written to.We have to initialize it ONCE

> error = red SWIGGLY LINE

> DUCK TYPING V/S STRUCTURAL TYPING
Duck typing checks behavior at runtime ("if it quacks like a duck"), while structural typing checks an object's shape at compile time. TypeScript uses structural typing.(PRECISE ANSWER)read about it.

> STRUCTURAL typing checks that is the STRUCTURE IS MAINTAINED or NOT ? IF YES then ok DOES NOT CARE ABOUT ADDITIONAL PROPERTIES,IF NO then throws error.IF BARE MINIMUM PROPERTY(s) is/are PRESENT then Structure is maintained and doesn’t matter what added after that
BUT WE CANNOT GIVE THAT TYPE TO THE OBJECT/VARIABLE we are defining coz it has a structure AND WE ARE ADDING NEW PROPERTIES IN OUR OBJECT which will give error
"mtlb hum ek object bnaaye uske ander woh bare minimum properties daale lekin type woh na de aur aur bhi properties daale baad mei uss object ka data uss dusre object mei daalde jiski type define kar di hai toh ERROR NHI AAYEGI kyoki bare minimum satisfied hai"

> KEYWORDS : readonly, partial, required

> <T> is a generic placeholder that allows you to write code which works with any type while preserving type safety.
<T> is a Generic Type Parameter

  Think of it as a placeholder for a type.

  Just like:

  function add(a, b) {
    return a + b;
  }

  Here a and b are placeholders for values.

  Similarly:

  function identity<T>(value: T): T {
    return value;
  }

  Here T is a placeholder for a type.

  function identity<T>(value: T): T {
    return value;
  }

  Usage:

  identity<string>("Tushar");
  identity<number>(22);

  TypeScript replaces T with:

  string

  or

  number

  respectively.

  Without Generics
  function identity(value: any): any {
      return value;
  }

  Problem:

  Type safety is lost
  With Generics
  function identity<T>(value: T): T {
      return value;
  }

  TypeScript knows:

  Input Type = Output Type
  Why T?

  T is just a convention.

  You can write:

  function identity<MyType>(value: MyType): MyType {
      return value;
  }

  or

  function identity<Data>(value: Data): Data {
      return value;
  }

  These are all valid.

  In Partial<T>
  type User = {
      name: string;
      age: number;
  }
  Partial<User>

  Here:

  T = User

  So TypeScript applies Partial to User.

  Result:

  {
      name?: string;
      age?: number;
  }
  Memory Trick
  <T>
  ↓
  A variable for a type

  Just like:
  x = variable for a value

  T = variable for a type
  One-Line Definition

  <T> is a generic placeholder that allows you to write code which works with any type while preserving type safety.

  "Generics = "Type ko parameter ki tarah pass karna"

> PARTIAL TYPE "mei dhyaan rakhne wali cheez ye hai ki bhai hum usme EMPTY OBJECT tak  PASS kar sakte hai which will create an issue to DHYAAN SE USE KARNA HAI"

> REQUIRED TYPE "makes all the properties REQUIRED/NECESSARY even if they all are OPTIONAL"

> PICK "hum selected properties pe hi kaam karte hai,mtlb type mei se SELECTED PROPERTIES hi chahiye hongi bs uske alawa aur kuch nhi" Pick<T,"K"> T is type and k is union of properties, Pick<NewChai,"name" | "price">

Partial
↓
Keep ALL fields
Make them OPTIONAL


Pick
↓
Choose SOME fields
Keep them REQUIRED

> ALWAYS put OPTIONAL PARAMETERS in the end of PARAMETERS LIST.

> TUPLE
 a tuple is an array with fixed size and known datatypes."Mtlb ke agar humne likha hai ki [string,number,string] toh ORDER WISE 1st pe string value 2nd pe number value and 3rd pe string value bs 3 hi define kari hai toh 3 hi values aayengi"

 "agar humne 3 types define ki hai ek tuple mei toh CORRESPONDING 3 values hi aayengi na zyada na kam"
 
 ORDER IS VERY IMPORTANT IN TUPLE WHEN IT COMES TO GIVE VALUES CORRESPONDING to the type defined

 "HUM TS MEI TUPLE MEI PUSH() OPERATION PERFORM KAR SAKTE HAI AUR WOH CHALEGA BHI KYOKI END OF TEH DAY TS BREAKSDOWN TO JS AND TypeScript cannot truly enforce a fixed-length array at runtime.BUT THE CATCH IS THE EXTRA ELEMENT WILL NOT GET THE SPACE INSIDE THE TUPLE mtlb memory mei jgh nhi milegi sirf unhi elements ko milegi jo define kiye hai"YOU CANNOT ACCESS THE EXTRA ELEMENT COZ IT DOES NOT EXIST IN MEMORY. WOOOOWWWW!!!!

 The structure of the tuple needs to stay the same (a string followed by a number), whereas the array can have any combination of the two types specified

> PRIVATE doesn't mean the data can never be revealed. It means only the class itself decides how and when it can be accessed

  The real purpose of private is:

  Hide implementation details
  +
  Protect data from direct modification
  +
  Force users to interact through controlled methods

> JS does not have traditional access modifiers but WE USE "#"for showing strict PRIVATE FIELDS and methods and "_VARIABLE" convention for showing PROTECTED FIELDS
BUT TS has access modifiers AND WE CAN ALSO USE "#" and "_VARIABLE" as at the end of the day it will convert into JS.



> Why i am able to reassign IN READONLY PROPERTY ?
The property can be assigned only once — either at declaration or inside the constructor.
 

Why is constructor assignment allowed?

Because when the object is being created:

const cup = new Cup(500);

the property hasn't been finalized yet.

TypeScript allows:

constructor(capacity: number) {
    this.Capacity = capacity;
}

as the initial assignment.



After construction finishes:

cup.Capacity = 1000;

❌ Not allowed.


Jab object banta hai:

const cup = new Cup(500);

Tab TypeScript bolta hai:

Object abhi create ho raha hai
Isliye readonly property ko value dena allowed hai

> get and set KEYWORDS

> STATIC members. STATIC MEMBERS are accessed using CLASSNAME.Static members are associated with THE CLASS NOT with the OBJECT OF the class.

> this in TS is same as JS.

> POLYMORPHISM - Polymorphism is the ability of the same method or function to perform different actions depending on the object or arguments.

 In JavaScript and TypeScript, polymorphism works in two primary ways:

  Method Overriding: A child class overrides a method of its parent class.Method overriding occurs when a subclass provides its own specific implementation of a method that is already defined in its parent class.

  Method Overloading (simulated): A function behaves differently based on the number or type of its arguments.JavaScript does not support true method overloading, but it can be simulated by handling different numbers or types of arguments within a single function.

> interface TeaMachine{
    start(): void,
    stop(): void
}

const MyteaMach: TeaMachine = {
    start(){
        console.log("start");
    },
    stop(){
        console.log("stop");
    }
}



This does not create an object.

It only says:

"Any object of type TeaMachine must have these methods."


An interface only defines the contract (method signatures), while the object or class provides the actual implementation of those methods.


const MyTeaMach: TeaMachine = {
    start() {
        console.log("start");
    },
    stop() {
        console.log("stop");
    }
};

Here you're saying:

"Create an object and make sure it satisfies the TeaMachine interface."

TypeScript checks:

Does it have start()? ✅
Does it have stop()? ✅

So it's valid.


An interface is conceptually very similar to an abstract class, but there are differences.

Interface
↓
Defines the contract

Abstract Class
↓
Defines the contract + can provide shared code

Polymorphism
↓
Different classes/objects follow the same contract but behave differently




So yes, interfaces and abstract classes are both tools used to achieve polymorphism, but they are not the same thing. The interface defines what must be done, while polymorphism is the ability to use different implementations through that common contract.

abstract class TeaMachine {
    abstract start(): void;
    abstract stop(): void;
}

This also forces child classes to implement these methods.

But an abstract class can also provide some implementation:

abstract class TeaMachine {
    abstract start(): void;

    clean() {
        console.log("Cleaning...");
    }
}

Interfaces cannot do that.

Interface      = Rules only
Abstract Class = Rules + Some Code
Polymorphism   = Same interface, different behavior.

> INDEX SIGNATURES
In TypeScript, index signatures allow you to define object types with dynamic keys, where the keys can be of a specific type, and the corresponding values can be of another type.This is particularly USEFUL WHEN you want to WORK WITH OBJECTS that have properties that are not known at compile-time but follow a specific pattern.

> MERGING IN INTERFACE
suppose there are two interfaces with the same name

interface User{
    name: string
}

interface User{
    age: number
}

const u: User = {
    // PROPERTIES OF BOTH THE USERS HAVE TO BE PRESENT HERE else IT WILL CREATE ERROR
    name: "Tushar",
    age: 22
}

> BIN folder (/bin) contains the BINARY FILES generally and LIB folder(/lib) contains source code files.
A bin (short for binary) folder contains the runnable, ready-to-use programs or apps. A lib (short for library) folder contains helper files, background code, and rules that those programs need to work properly.

The bin folder is the finished meal you serve to your guests. You can eat it right away.
The lib folder is the pantry full of raw spices, dough, and sauces.
You can't eat the pantry, but the chef needs these helper items to make the meal

.d = declaration file
-D(FLAG) = used for showing DEV DEPENDENCY

HUM KOI BHI EXTERNAL package DOWNLOAD KARTE HAI LIKE TypeScript,AXIOS AND ALL. TOH PACKAGE MEI HUME EK BIN FOLDER MILTA HAI AUR EK LIB FOLDER BIN MEI READY HOTA HAI SAB AUR LIB MEI SAB declaration FILES,HELPERS,ERRORS,HINTS YE SAB HOTE HAI JO HMAARI HELP KARTE HAI CODE LIKHNE MEI

> WHENEVER YOU INSTALL AN EXTERNAL LIBRARY ALWAYS INSTALL THEIR TYPES TOO.
>import type { AxiosResponse,isAxiosError } from "axios";
// YOU have to USE "IMPORT TYPE" when you are importing types of any external library 

> GENERICS : READ FROM OFFICIAL DOCS and gpt




> Sometimes external libraries OR packages does not provide their TYPES so we install it using "npm install -D @types/LIB_NAME" or we configure it in declaration file(.d.ts)

> WE ALSO IMPORT THE TYPES WHEN WORKING WITH WEB REQUESTS AND ALL.LIKE in "Axios" we have "AxiosResponse" which tells that the type of the datatype is "AxiosResponse" only like you can say these are datatypes inbuilt in the external data fetching libraries.

Axios is a popular, open-source JavaScript library used to send HTTP requests from web browsers and Node.js environments. It is widely used by developers to communicate with APIs, as it simplifies tasks like fetching data, handling errors, and automatically transforming JSON data.



> Why not just write Todo? WHY WE HAVE USED AXIOSREPSONSE<todo> ?   

Because the response itself isn't a Todo.

The response looks like this:

{
   data: Todo,
   status: 200,
   headers: ...
}

So

AxiosResponse<Todo>

means

"This response object has a data property whose type is Todo."

If you wrote

const response: Todo

that would be incorrect, because response also contains status, headers, config, etc.

The generic tells TypeScript what's inside data.

AxiosResponse<Todo> → data is a Todo.
AxiosResponse<User> → data is a User.
AxiosResponse<Product[]> → data is an array of Product.

That's why AxiosResponse is generic: the wrapper is constant, but the payload (data) can be any type.

We didn't make AxiosResponse<T>; Axios already provides it. We use AxiosResponse<T> to specify the type of the data field inside the response.
We use AxiosResponse<T> because the data field can contain different types depending on the API, and the generic tells TypeScript exactly what type data is.




> In JavaScript, ANY FUNCTION THAT DOES NOT EXPLICITLY RETURN A VALUE AUTOMATICALLY RETURNS UNDEFINED.
function hello(){
    return ("hello promise");
}


const p = new Promise((resolve, reject) => {
    setTimeout(() => {
        resolve(hello())
    }, 2000);
});

p.then((res)=>{
    console.log(res);
    // at first it was returning an extra "undefined" coz in js if a function expplicityy does not return anything then it returns UNDEFINED.We should execute the function here not in the "resolve()"
})
.catch((err)=>{
    console.log(err);
})


1. resolve(hello()) -> Result मिलता हैयहाँ आप फ़ंक्शन को तुरंत चला देते हैं। फ़ंक्शन जो भी return करेगा (जैसे कोई text, number या object), वही .then() के पास जाएगा।.then(res) में क्या मिलेगा: फ़ंक्शन का आउटपुट (जैसे: "Success")।

2. resolve(hello) -> Reference मिलता हैयहाँ फ़ंक्शन चलता नहीं है, बल्कि .then() के पास ट्रांसफर हो जाता है। .then() को फ़ंक्शन का केवल एक शॉर्टकट (Reference) मिलता है।.then(res) में क्या मिलेगा: पूरा फ़ंक्शन खुद function hello() { ... }।result देखने के लिए: आपको .then() के अंदर जाकर res() लिखकर उसे खुद चलाना पड़ेगा।



> Read about Axios too.
> computer networks and how internet works
> Error in JS(ERROR OBJECT)


